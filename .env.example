# ---------------------------------------------------------------------------
# Backend (backend/.env)
# ---------------------------------------------------------------------------

# MongoDB connection string (MongoDB Atlas or self-hosted)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/schdle-admin?retryWrites=true&w=majority

# Port the Express server listens on (optional, defaults to 5000)
PORT=5000

# ---------------------------------------------------------------------------
# Frontend (frontend/.env)
# ---------------------------------------------------------------------------

# Base URL of the backend API (no trailing slash).
# Local development example:
# REACT_APP_API_URL=http://localhost:5000
#
# Production (Vercel): leave this UNSET/empty. vercel.json already proxies
# /api/* on the same domain to the backend, so the frontend should call
# relative paths. Setting this to http://localhost:5000 (or any unreachable
# URL) on Vercel will cause "Failed to fetch" errors in the deployed app.
REACT_APP_API_URL=
